---
import PageLayout from '../layouts/PageLayout.astro';
import { siteUpdates } from "../data/update";
const pageTitle = "NAOMINA121";
const update = siteUpdates;
const pubDate = "2025-12-01";
const modDate = "2025-12-10";
const image = {
    url: '/images/nao2.jpg',
    alt: 'ナオの公式サイトのイメージ画像',
};
const description = "ナオの公式サイト、NAOMINA121へようこそ。メンタルヘルス、情報リテラシー、ブログ、ポッドキャスト、資料室など、多彩なコンテンツを提供しています。ナオの活動や考え方について詳しく知ることができます。";
---

<PageLayout pageTitle={pageTitle} pubDate={pubDate} modDate={modDate} image={image} description={description}>
<h2>はじめに</h2>
<p>このサイトは、<a href="/profile/about" class="link">ナオ</a>が運営するナオの公式サイトです。</p>
<h2>発信コンテンツ</h2>

<p>サイト内にあるコンテンツについて紹介していきます。</p>

<table class="m">
<thead>
<tr>
<th>項目</th>
<th>説明</th>
</tr>
</thead>
<tbody>
<tr>
<td>当サイトについて</td>
<td>運営方針やサイトの目的について説明しています。</td>
</tr>
<tr>
<td>プロフィール</td>
<td>ナオの基本情報です</td>
</tr>
<tr>
<td>メンタルヘルス</td>
<td>ナオがまとめたメンタルヘルスに関する情報を載せています。</td>
</tr>
<tr>
<td>情報リテラシー</td>
<td>ナオがまとめた情報リテラシーに関する情報を載せています。</td>
</tr>
<tr>
<td>ナオのブログ</td>
<td>ナオのブログです。</td>
</tr>
<tr>
<td>ナオの音声ブログ</td>
<td>ナオの音声配信用のブログです。</td>
</tr>
<tr>
<td>ナオの資料室</td>
<td>ナオが作成した資料を掲載しています。</td>
</tr>
<tr>
<td>ナオの活動拠点</td>
<td>外部サイトのナオコンテンツです。</td>
</tr>
</tbody>
</table>

<h2>お知らせ</h2>
<div class="box">
  <h3 style="font-size:18px; border-bottom:1px solid #727272; margin:0 0 10px 0px; padding: 0px 0px 10px;">
    📰更新履歴
  </h3>
  <p>無理のない更新頻度を目指しているため、記事の更新ペースは、<u class="orange">週に一回の頻度</u>を目指しています。</p>
  <p>記事の内容に関して、ご質問やご意見がございましたら、<a href="/contact/" class="link">お問い合わせページ</a>からご連絡ください。</p>

  <p class="mt-2 font-semibold" style="color:#ef7e7e; font-weight:500; padding-top:10px; margin-top:10px; border-top:1px solid #727272">重要事項の更新履歴</p>
  <ul style="font-weight:500;">
    {update.map((update) => {
      return update.category==="Policy" && (
      <li class="my-1">
        <time style="color:#ff9a4b;" class="text-sm" datetime={`${update.date}T00:00:00+09:00`}>{update.date}</time>
        {' : '}
        <a href={`/updates/${update.slug}`} class="text-[#c9c9c9] hover:underline hover:underline-offset-4">
          {update.title}
        </a>
      </li>
    )
    }).filter(Boolean).slice(0, 3)}
  </ul>
   <p class="mt-2 font-semibold" style="color:#ceb300; font-weight:500; padding-top:10px; margin-top:10px; border-top:1px solid #727272">サイトの更新履歴</p>
  <ul style="font-weight:500;">
    {update.map((update) => {
      return update.category==="Technical" && (
      <li class="my-1">
        <time style="color:#ff9a4b;" class="text-sm" datetime={`${update.date}T00:00:00+09:00`}>{update.date}</time>
        {' : '}
        <a href={`/updates/${update.slug}`} class="text-[#c9c9c9] hover:underline hover:underline-offset-4">
          {update.title}
        </a>
      </li>
    )
    }).filter(Boolean).slice(0, 3)}
  </ul>
  <p class="mt-2 font-semibold" style="font-weight:500; color:#cdcdcd; padding-top:10px; margin-top:10px; border-top:1px solid #727272">記事の更新履歴</p>

  <ul style="font-weight:500;">
    {update.map((update) => {
      return update.category==="Article" && (
      <li class="my-1">
        <time style="color:#ff9a4b;" class="font-weight:500; text-sm" datetime={`${update.date}T00:00:00+09:00`}>{update.date}</time>
        {' : '}
        <a href={`/updates/${update.slug}`} class="text-[#c9c9c9] hover:underline hover:underline-offset-4">
          {update.title}
        </a>
      </li>
    )
    }).filter(Boolean).slice(0, 3)}
  </ul>
  <p style="margin-top:10px; text-align:right;">
    <a href="/updates/" style="font-weight:100;" class="gold underline hover:text-[#fff]">→ お知らせ一覧を見る</a>
  </p>
</div>
</PageLayout>